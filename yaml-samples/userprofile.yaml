apiVersion: apps/v1
kind: Deployment
metadata:
  name: userprofile
spec:
  replicas: 2
  selector:
    matchLabels:
      app: userprofile
  template:
    metadata:
      labels:
        app: userprofile
    spec:
      nodeSelector:
        "beta.kubernetes.io/os": linux
      containers:
      - name: userprofile
        image: registryatk4536.azurecr.io/tripinsights/userprofile:1.0
        env:
        - name: PORT
          value: "8084"
        - name: WEB_PORT
          value: "8084"
        - name: SQL_USER
          value: "<not-set>"
        - name: SQL_PASSWORD
          value: "<not-set>"
        - name: SQL_SERVER
          value: "<not-set>t"
        - name: SQL_DBNAME
          value: "<not-set>"
        ports:
        - containerPort: 8084
---
apiVersion: v1
kind: Service
metadata:
  name: userprofile
  labels:
    app: userprofile
spec:
  ports:
  - port: 80
    targetPort: 8084
    protocol: TCP
    name: http
  selector:
    app: userprofile
