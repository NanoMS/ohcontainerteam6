apiVersion: apps/v1
kind: Deployment
metadata:
  name: poi
spec:
  replicas: 2
  selector:
    matchLabels:
      app: poi
  template:
    metadata:
      labels:
        app: poi
    spec:
      containers:
      - name: poi
        image: registryatk4536.azurecr.io/tripinsights/poi:1.0
        env:
        - name: PORT
          value: "8081"
        - name: WEB_PORT
          value: "8081"
        - name: SQL_USER
          value: "<not-set>"
        - name: SQL_PASSWORD
          value: "<not-set>"
        - name: SQL_SERVER
          value: "<not-set>"
        - name: SQL_DBNAME
          value: "<not-set>"
        ports:
        - containerPort: 8081
---
apiVersion: v1
kind: Service
metadata:
  name: poi-svc
spec:
  ports:
  - port: 80
    targetPort: 8081
    protocol: TCP
    name: http
  selector:
    app: poi
