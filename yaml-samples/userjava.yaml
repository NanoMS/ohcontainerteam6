apiVersion: apps/v1
kind: Deployment
metadata:
  name: userjava
spec:
  replicas: 2
  selector:
    matchLabels:
      app: userjava
  template:
    metadata:
      labels:
        app: userjava
    spec:
      containers:
      - name: userjava
        image: registryatk4536.azurecr.io/tripinsights/userjava:1.0
        env:
        - name: PORT
          value: "80"
        - name: WEB_PORT
          value: "80"
        - name: SQL_USER
          value: "<not-set>"
        - name: SQL_PASSWORD
          value: "<not-set>"
        - name: SQL_SERVER
          value: "<not-set>.database.windows.net"
        - name: SQL_DBNAME
          value: "<not-set>"
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: userjava-svc
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: userjava
